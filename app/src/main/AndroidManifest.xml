<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wellnessapp">


    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_voley_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_voley_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".RecuperarCuenta"></activity>
        <activity android:name=".pWellnesActivity" />
        <activity android:name=".pPSEActivity" />
        <activity android:name=".EditarActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".PSE" />
        <activity android:name=".WellnessActivity" />
        <activity android:name=".Registrar" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>

    </application>

</manifest>